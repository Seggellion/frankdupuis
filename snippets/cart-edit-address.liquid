
<style>
  .required{
    color:red;
    font-size: 1.4rem;
    font-family: Arial;
    margin-left: 2px;
  }
</style>

{%- comment -%} You have to edit the default one by default | Added by Dustin @shopify on Jan. 22, 2023 {%- endcomment -%}
{% assign address = customer.addresses[0] %}
{%- form 'customer_address', address -%}
  <div class="field">
    <input type="text" id="AddressCompany_{{ form.id }}" name="address[company]" value="{{ form.company }}" autocomplete="organization" placeholder="Optional">
    <label for="AddressCompany_{{ form.id }}">{{ 'customer.addresses.company' | t }}</label>
  </div>
  <div class="field">
    <input type="text" id="AddressAddress1_{{ form.id }}" name="address[address1]" value="{{ form.address1 }}" autocomplete="address-line1" required>
    <label for="AddressAddress1_{{ form.id }}">{{ 'customer.addresses.address1' | t }}<span class="required">*</span></label>
  </div>
  <div class="field">
    <input type="text" id="AddressAddress2_{{ form.id }}" name="address[address2]" value="{{ form.address2 }}" autocomplete="address-line2" placeholder="Optional" >
    <label for="AddressAddress2_{{ form.id }}">{{ 'customer.addresses.address2' | t }}</label>
  </div>
  <div class="field">
    <input type="text" id="AddressCity_{{ form.id }}" name="address[city]" value="{{ form.city }}" autocomplete="city" required>
    <label for="AddressCity_{{ form.id }}">{{ 'customer.addresses.city' | t }}<span class="required">*</span></label>
  </div>
  <div class="field">
   <input type="hidden" id="AddressCountry" name="address[country]" value="Canada">
    <input type="hidden" id="AddressCountry" name="address[province]" value="Ontario">
  </div>
  <div class="field">
    <input type="text" id="AddressZip_{{ form.id }}" name="address[zip]" value="{{ form.zip }}" autocapitalize="characters" autocomplete="postal-code" required>
    <label for="AddressZip_{{ form.id }}">{{ 'customer.addresses.zip' | t }}<span class="required">*</span></label>
  </div>
  <div class="field">
    <input type="tel" id="AddressPhone_{{ form.id }}"  name="address[phone]" value="{{ customer.phone }}" placeholder="{{ customer.phone }}" autocomplete="tel" required>
    <label for="AddressPhone_{{ form.id }}">{{ 'customer.addresses.phone' | t }}<span class="required">*</span></label>
  </div>
  <div>
    {{ form.set_as_default_checkbox }}
    <label for="address_default_address_{{ form.id }}">
      {{ 'customer.addresses.set_default' | t }}
    </label>
  </div>
  <div>
    <button>Continue</button>    
  </div>
{%- endform -%}
{%- comment -%} @MC:EOL | Dustin @shopify on Jan. 22, 2023 {%- endcomment -%}